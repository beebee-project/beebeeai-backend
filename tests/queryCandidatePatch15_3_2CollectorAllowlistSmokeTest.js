"use strict";
const assert=require("assert");const s=require("./queryCandidatePatch15_3_2TestSupport");const {hashQueryCandidatePlannerInternalCanarySubject}=require("../automation/queryCandidatePlannerInternalCanarySubject");const {recordQueryCandidatePlannerRealShadowObservation}=require("../automation/queryCandidatePlannerRealShadowEvidenceCollector");(async()=>{const store=s.createMemoryRealShadowEvidenceStore();const accountId="internal";const subject=hashQueryCandidatePlannerInternalCanarySubject({accountId});const fp=s.hash("fp");const registry={version:"query_candidate_planner_real_shadow_case_registry_v1",registryId:"r",cases:[{caseId:s.accuracyDataset().cases[0].caseId,scenarioId:"s",requestFingerprintSha256:fp,uploadFingerprintSha256:"",expectedColdCostMicrousd:240,modelId:"semantic_profiler_default"}]};const env={QUERY_CANDIDATE_PLANNER_REAL_SHADOW_EVIDENCE_ENABLED:"1",QUERY_CANDIDATE_PLANNER_REAL_SHADOW_EVIDENCE_SECRET:"z".repeat(64),QUERY_CANDIDATE_PLANNER_PROMOTION_ALLOWLIST_SHA256:subject,QUERY_CANDIDATE_PLANNER_REAL_SHADOW_CASE_REGISTRY_JSON:JSON.stringify(registry)};const observation={version:"query_candidate_planner_api_shadow_observation_v1",status:"COMPLETED",reason:"SHADOW_COMPLETED",requestFingerprintSha256:fp,primaryResponseUnchanged:true,latencyMs:10,comparison:{verdict:"MATCH",counts:{primary:1,shadow:1,shared:1},metrics:{exactOrder:true,top1Same:true,jaccard:1,rankAgreement:1}},guardrails:{shadowOnly:true,primaryResponseAuthority:true,productionCandidateMerge:false,productionReadyAssignment:false,productionRouteChanged:false},privacy:{}};const prediction=s.predictionForCase(s.accuracyDataset().cases[0]);const capture={resolution:{status:"SHADOW_COMPLETED",candidates:prediction.candidates,businessDomainProfile:{primaryDomain:prediction.domain,datasetIntent:prediction.intent,reviewRequired:prediction.reviewRequired},fallback:{applied:prediction.fallbackApplied,reason:prediction.fallbackReason},unsupportedRejected:prediction.unsupportedRejected,plannerResolution:{invocation:{providerCallCount:1,modelId:"semantic_profiler_default",inputTokens:1000,outputTokens:200,observedCostMicrousd:240}},cache:{readAttempted:true,hit:false,level:"MISS",writeAttempted:true,writeSucceeded:true}}};const result=await recordQueryCandidatePlannerRealShadowObservation(observation,{req:{user:{accountId}}},{env,store,capture,now:()=>Date.parse("2026-08-05T00:00:00Z")});assert.strictEqual(result.stored,true);assert.strictEqual((await store.list()).length,1);console.log("PASS query candidate patch15.3.2 allowlisted actual shadow collection smoke");})().catch(e=>{throw e;});
