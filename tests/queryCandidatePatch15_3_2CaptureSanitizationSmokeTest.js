"use strict";
const assert=require("assert");const {sanitizeShadowResolutionForEvidence}=require("../automation/queryCandidatePlannerRealShadowCaptureBridge");const r=sanitizeShadowResolutionForEvidence({status:"SHADOW_COMPLETED",items:[{candidateId:"sales_summary",title:"not stored",rank:1}],semanticProfile:{primaryDomain:"SALES_REVENUE",datasetIntent:"TRANSACTION_ANALYSIS"},providerCallCount:1});assert.strictEqual(r.candidates[0].candidateId,"sales_summary");assert.strictEqual(r.candidates[0].title,undefined);assert.strictEqual(r.privacy.rawProviderResponseIncluded,false);console.log("PASS query candidate patch15.3.2 shadow capture sanitization smoke");
